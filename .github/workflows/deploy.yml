name: CI/CD for Vite + React with Docker

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🔽 Клонируем репозиторий
        uses: actions/checkout@v3

      - name: 🔧 Логинимся в сервер через SSH и деплоим
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/user/app-folder
            git pull origin main
            docker stop client || true
            docker rm client || true
            docker rmi client:latest || true
            docker build -t client:latest .
            docker run -d --name client -p 80:80 client:latest
